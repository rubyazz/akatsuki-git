# This directory holds the platform-specific `akatsuki-backend` binary at package
# time (populated by scripts/package.mjs / CI). The binaries themselves are
# gitignored; this file only exists so the directory is tracked.
